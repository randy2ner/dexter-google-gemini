# Compass participant management schema-v2 fixture

This disconnected fictional fixture supports the planned `CPM-V2-001` scenario. It contains no work data, credentials, tenant identifiers, live links, personal data, or expected runtime output.

The fixture demonstrates a structurally valid positive baseline only. Negative variants must be materialized separately and recorded before execution. Do not treat this fixture or its expected semantics as evidence that a Skill works.
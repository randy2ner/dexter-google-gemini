# Compass Topic attention-state schema-v2 fixture

This disconnected fictional fixture supports planned scenario `ctas-v2-002`. It contains active Topics for `action`, `waiting`, and `observing`, plus one archived successful Topic retaining `waiting`.

The fixture is a static positive input. It does not demonstrate interaction, transitions, invalid-case rejection, or Skill behavior. Create negative variants only in a disposable copy.